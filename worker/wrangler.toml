name = "finlens-ai"
main = "src/index.ts"
compatibility_date = "2026-08-21"

[vars]
# Replace after you create Firebase. This is not a secret.
FIREBASE_PROJECT_ID = "YOUR_FIREBASE_PROJECT_ID"
# GitHub Pages URL plus localhost for development. Comma-separated.
ALLOWED_ORIGINS = "https://rk-1707.github.io,http://localhost:5173,http://127.0.0.1:5173"
# Free-tier-capable lightweight model. Can be changed later without frontend changes.
GEMINI_MODEL = "gemini-3.5-flash-lite"
